
// call script to seed data with cli
// check that the data is added correctly

import { exec } from "child_process"

describe('initial data seeder', () => {
  
  afterAll(() => {

  })
  it('initial data add data correctly when db is empty', () => {

  })
})